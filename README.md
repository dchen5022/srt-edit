# srt-edit
Simple CLI for applying timing offsets to SRT files
# Usage
Provide the input and output filepaths and the offset to be applied in milliseconds.
`srtedit <input-filepath> <output-filepath> <offset-ms>`
